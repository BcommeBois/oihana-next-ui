const scheduler =
{
    fr :
    {
        title       : 'Planning' ,
        description : "La famille calendrier d'événements, bâtie sur un noyau sans interface : un adaptateur schema.org qui lit les propriétés plutôt que les types, la récurrence déclarative, les colonnes de chevauchement et un axe de temps." ,
    } ,
    en :
    {
        title       : 'Scheduler' ,
        description : 'The event calendar family, built on a headless core: a schema.org adapter that reads properties rather than types, declarative recurrence, overlap columns and a time axis.' ,
    } ,
} ;

export default scheduler ;
