/**
 * Default labels of {@link module:components/Alert}.
 *
 * Only the close cross needs one : everything else an alert shows is the text it
 * was given. A cross with no name is announced as « button », which tells a
 * reader neither what it does nor what it will dismiss.
 */
const alert =
{
    fr :
    {
        close : 'Fermer' ,
    } ,
    en :
    {
        close : 'Close' ,
    } ,
} ;

export default alert ;
