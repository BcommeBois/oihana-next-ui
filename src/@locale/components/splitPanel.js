/**
 * Default labels of the `SplitPanel` component.
 *
 * `close` names the dismiss surface — both the overlay and the corner
 * button, which drive the very same toggle.
 */
const splitPanel =
{
    fr :
    {
        close : 'Fermer le panneau' ,
    } ,

    en :
    {
        close : 'Close panel' ,
    } ,
} ;

export default splitPanel ;
